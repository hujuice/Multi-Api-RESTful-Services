Have some class in 'resources/'.
Prepare a simple configuration file for each resource.

Prepare a master configuration file, config.ini.

Add some URL rewriting rule to your Apache (see example).

Prepare a very light index.php

That's all!
